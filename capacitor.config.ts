import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.readhandy.com',
  appName: 'Read Handy',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
